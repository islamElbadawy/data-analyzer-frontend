import { FilterChartDataPipe } from './filter-chart-data.pipe';

describe('FilterChartDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChartDataPipe();
    expect(pipe).toBeTruthy();
  });
});
